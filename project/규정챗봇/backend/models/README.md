# Local Model Files

This directory is reserved for local LLM model files used during development.

Model binaries are intentionally not committed to GitHub because they are large
and may have separate license or distribution requirements.

For a local run, place the required model file in this directory and configure
the matching environment variable in `.env`.
